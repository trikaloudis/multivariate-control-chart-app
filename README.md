The code will generate a multivariate control chart according to Hotelling’s T2 statistic. 
It will also generate a model-driven multivariate control chart (MDMVCC) with automatic model selection based on principal components analysis (PCA), 
a control chart on the normalized distance from each data value to the PCA model (DModX), and a contribution proportion plot.
The app will for an excel or CSV file with data. Column A is the "Index" of data and Column B is the "Date". All other columns are the various parameters. 
